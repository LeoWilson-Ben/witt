# 安全策略

## 支持范围

安全修复优先面向 `main` 分支。自托管部署者应保持 Codex CLI、Node.js、Android System WebView、Nginx 和操作系统安全更新。

## 私密报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。不要为未修复的认证绕过、远程命令执行、路径穿越、凭据泄露或跨用户数据访问创建公开 Issue。

报告应包含：

- 受影响版本或提交
- 最小复现步骤
- 影响范围与前置条件
- 已做的缓解措施
- 经过脱敏的日志或截图

不要发送真实令牌、邀请码、密码、私钥、数据库或用户文件。维护者会尽快确认报告，并在确认修复及披露时间后协调发布。

## 部署者责任

- 只通过 HTTPS 暴露服务，Node.js API 保持监听回环地址。
- 使用专用系统用户和最小权限 sudo 规则。
- 令牌、签名配置和 Codex 登录目录不得进入 Git、APK 或 Web 静态目录。
- 定期轮换兼容令牌并停用不再使用的设备。
- 对会话数据库、认证数据库和用户文件进行加密备份。
- 公开仓库前扫描完整 Git 历史、Actions 日志、Release 附件和构建产物。

本项目可以代表用户执行高权限操作。部署者必须根据自己的威胁模型配置审批策略、网络边界和备份策略。
